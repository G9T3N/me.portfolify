# Contributing

1. Create a feature branch
2. Make your changes
3. Ensure the project builds, lints, and typechecks (`pnpm test`, `pnpm build`)
4. Commit your changes using conventional commit formats (enforced by commitlint)
5. Submit a pull request
