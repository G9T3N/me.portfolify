{
  "name": "@mrerr/database",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "generate": "drizzle-kit generate:pg",
    "migrate": "drizzle-kit migrate",
    "seed": "tsx src/seed/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "drizzle-orm": "^0.30.10",
    "postgres": "^3.4.4",
    "arktype": "^2.2.2",
    "@mrerr/env": "workspace:*",
    "@mrerr/domain": "workspace:*"
  },
  "devDependencies": {
    "@mrerr/typescript-config": "workspace:*",
    "drizzle-kit": "^0.20.18",
    "typescript": "^5.0.0",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
